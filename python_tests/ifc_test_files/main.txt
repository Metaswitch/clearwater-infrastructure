
This service profile applies to sip:2012012001@test.cw-ngv.com and tel:+2012012001 and sip:2125550822@mgm.cw-ngv.com

	Priority 1010:
	If
	- (Method is INVITE) AND (Diversion header is not present) AND (Contact header does not match ".*xs.*\.tas\.ims\.cw-ngv.*") AND (Session case is originating-registered)
	then invoke sip:tas.cw-ngv.com;lr;call=orig

	Priority 1012:
	If
	- (Method is INVITE) AND (Feature-Caps header is not present) AND (Session case is terminating-unregistered)
	then invoke sip:tas.cw-ngv.com;lr;call=term_unregistered

	Priority 1014:
	If
	- (Method is REGISTER)
	then invoke sip:tas.cw-ngv.com;lr

	Priority 1016:
	If
	- (Method is INVITE) AND (Feature-Caps header is not present) AND (Session case is terminating-registered)
	then invoke sip:tas.cw-ngv.com;lr;call=term_registered

	Priority 1018:
	If
	- (Method is MESSAGE) AND (Content-Type header matches "application/vnd.3gpp.sms") AND (Request-URI is not "ipsm-rstn.ims.cw-ngv.com") AND (Request-URI is not "ipsm-sob.ims.cw-ngv.com") AND (Session case is originating-registered)
	then invoke sip:ipsm.ims.cw-ngv.com

	Priority 1020:
	If
	- (Method is SUBSCRIBE) AND (Event header does not match "reg")
	then invoke sip:tas.cw-ngv.com
